# אמא במשרד – מדריך משתמש (User Manual)

מדריך שימוש בפלטפורמת ההזמנות – ללקוחות ולמנהלים.

---

## חלק א׳ – ללקוח (הזמנה מהאתר)

### 1. כניסה לאתר
- פתחו את כתובת האתר בדפדפן (למשל: `https://your-app.up.railway.app`).

### 2. בחירת מוצר
- בעמוד הבית מוצגים **3 גדלי מגש**: 35 יח׳, 50 יח׳, 70 יח׳.
- כל מגש מציג: גודל, מתאים ל־X אנשים, מחיר.
- לחצו **"להזמנה"** על המגש הרצוי.

### 3. טופס ההזמנה
- **מוצר** – נבחר לפי המגש (ניתן לשנות: 35 / 50 / 70 יח׳).
- **כמות קופסאות** – מספר הקופסאות (ברירת מחדל: 1).
- **שם מלא** – חובה.
- **טלפון** – חובה.
- **אימייל** – אופציונלי (לחשבונית).
- **כתובת משרד למשלוח / שם משרד** – חובה.
- **תאריך משלוח** – חובה; לפחות 3 ימים מראש.
- **הערות** – אופציונלי.

המערכת מציגה: סה״כ יחידות, מחיר לקופסה, **סה״כ לתשלום**.

### 4. שליחת ההזמנה
- מלאו את השדות ולחצו **"שליחת הזמנה"**.
- לאחר שליחה מוצגת הודעת הצלחה. התשלום (bit) מתבצע **מחוץ לאתר** – לפי ההנחיות שתקבלו מהעסק.

---

## חלק ב׳ – למנהל (לוח הניהול)

### 1. כניסה ללוח המנהל
- גלשו לכתובת: **כתובת-האתר/manager** (למשל: `https://your-app.up.railway.app/manager`).
- הזינו את **סיסמת המנהל** ולחצו **"התחבר"**.
- סיסמת ברירת מחדל מוגדרת בקוד (`src/lib/auth.ts`) – **חשוב לשנות לפני שימוש באינטרנט**.

### 2. מסך הלוח (Header)
- **יציאה** – ניתוק מהמערכת.
- **חזרה לאתר** – מעבר לאתר הלקוחות.
- **סטטוס** – מספר הזמנות לפי סטטוס: **חדש**, **אושר**, **נמסר**.

### 3. סינון ומיון
- **מיון לפי:** תאריך (כרונולוגי) / שם (א״ב) / כתובת (א״ב).
- **הצג לי:** הכל / חדש / אושר / נמסר.
- **מוצר:** הכל / 35 יח׳ / 50 יח׳ / 70 יח׳.

### 4. לוח שנה (בצד)
- **לוח שנה** – הזמנות לפי תאריך משלוח.
- לחיצה על יום: בחירת **טווח תאריכים** (יום אחד או כמה ימים).
- **"הצג הכל"** – ביטול סינון לפי תאריך.
- ימים עם הזמנות מסומנים; התאריכים שבטווח מודגשים.

### 5. הזמנות פעילות
- רשימת הזמנות במצב **חדש** או **אושר** (לא נמסר).
- לכל הזמנה מוצגים:
  - **מס׳ הזמנה**, סטטוס קבצים (ירוק = הכל קיים, אדום = חסר קובץ).
  - **סטטוס** – רשימה: חדש / אושר / נמסר (ניתן לשינוי).
  - **הדפס** – הדפסת פרטי ההזמנה.
  - **מוצר, כמות קופסאות, סה״כ יחידות, מחיר לקופסה, סה״כ מחיר**.
  - **עדכון מחיר לקופסה** – שדה + כפתור **"שמור"** (עדכון מחיר להזמנה).
  - פרטי לקוח: שם, טלפון, אימייל, כתובת, תאריך משלוח, הערות.
  - **קבצים מצורפים** – העלאת קבצים (צילום bit, חשבונית, אחר), צפייה והורדה, מחיקה.

### 6. היסטוריה
- לחיצה על **"היסטוריה"** מציגה הזמנות במצב **נמסר**.
- אותן פעולות (סטטוס, הדפס, קבצים, עדכון מחיר) זמינות גם בהיסטוריה.

### 7. דוחות
- לחיצה על **"דוחות"** מציגה:
  - **הכנסות לפי מוצר** – סכום ומספר הזמנות ל־35 / 50 / 70 יח׳.
  - **סה״כ יחידות לפי מוצר**.
  - **סה״כ הכנסות** ו־**ממוצע להזמנה**.
- **"ייצוא לאקסל"** – הורדת קובץ Excel עם גיליון סיכום וגיליון הזמנות מפורט.

### 8. יציאה
- **יציאה** header מנתקת מהמערכת; יש להזין שוב סיסמה בכניסה הבאה.

---

## שאלות נפוצות

**איך משנים את סיסמת המנהל?**  
בקוד: קובץ `src/lib/auth.ts` – עדכון הסיסמה ושמירה, ואז פריסה מחדש (או הרצה מחדש אם מקומית).

**הזמנות לא נשמרות אחרי רענון?**  
בהרצה מקומית – וודא ש־`npm run dev` רץ ושהתיקייה `data/` קיימת. באינטרנט – וודא שהאחסון (Volume/דיסק) מחובר לתיקיית `data/` לפי [DEPLOYMENT.md](./DEPLOYMENT.md).

**איך מגבים הזמנות וקבצים?**  
העתקת תיקיית `data/` (כולל `orders.json` ו־`uploads/`) לגיבוי חיצוני. ייצוא לאקסל שומר סיכום ורשימת הזמנות, אך לא את הקבצים המצורפים.

**האתר לא נטען באינטרנט.**  
בדקו שהפרויקט פרוס לפי [DEPLOYMENT.md](./DEPLOYMENT.md), ושכתובת ה־URL נכונה (כולל `/manager` ללוח המנהל).

---

*עוד מידע טכני: [README.md](./README.md). פריסה לאינטרנט: [DEPLOYMENT.md](./DEPLOYMENT.md).*
